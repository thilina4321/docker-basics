// from this we can create our own utility container 

// From this docker-compose.yaml we can create the our own npm 

// run this with => 
  // docker-compose run --rm <container_name> init
  // docker-compose run --rm npm init